# permissions for end users to view measurements.
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/name: chantico
    app.kubernetes.io/managed-by: kustomize
  name: measurement-viewer-role
rules:
- apiGroups:
  - chantico.ci.tno.nl
  resources:
  - measurements
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - chantico.ci.tno.nl
  resources:
  - measurements/status
  verbs:
  - get
