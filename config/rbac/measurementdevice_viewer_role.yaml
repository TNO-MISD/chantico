# permissions for end users to view measurementdevices.
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/name: chantico
    app.kubernetes.io/managed-by: kustomize
  name: measurementdevice-viewer-role
rules:
- apiGroups:
  - chantico.ci.tno.nl
  resources:
  - measurementdevices
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - chantico.ci.tno.nl
  resources:
  - measurementdevices/status
  verbs:
  - get
