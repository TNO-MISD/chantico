# permissions for end users to edit pdus.
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/name: chantico
    app.kubernetes.io/managed-by: kustomize
  name: pdu-editor-role
rules:
- apiGroups:
  - chantico.ci.tno.nl
  resources:
  - pdus
  verbs:
  - create
  - delete
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - chantico.ci.tno.nl
  resources:
  - pdus/status
  verbs:
  - get
