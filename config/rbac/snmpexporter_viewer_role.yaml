# permissions for end users to view snmpexporters.
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/name: chantico
    app.kubernetes.io/managed-by: kustomize
  name: snmpexporter-viewer-role
rules:
- apiGroups:
  - chantico.ci.tno.nl
  resources:
  - snmpexporters
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - chantico.ci.tno.nl
  resources:
  - snmpexporters/status
  verbs:
  - get
