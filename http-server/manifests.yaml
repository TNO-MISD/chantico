---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: datacenterresources.chantico.ci.tno.nl
spec:
  group: chantico.ci.tno.nl
  names:
    kind: DataCenterResource
    listKind: DataCenterResourceList
    plural: datacenterresources
    singular: datacenterresource
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: DataCenterResource is the Schema for the datacenterresources
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of DataCenterResource
            properties:
              parent:
                items:
                  type: string
                type: array
              physicalMeasurements:
                items:
                  type: string
                type: array
              type:
                type: string
            required:
            - type
            type: object
          status:
            description: status defines the observed state of DataCenterResource
            properties:
              errorMessage:
                type: string
              state:
                type: string
              updateGeneration:
                format: int64
                type: integer
              updateTime:
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: chantico.ci.tno.nl/v1alpha1
kind: DataCenterResource
metadata:
  name: pdu1
  namespace: default
spec:
  type: pdu
  physicalMeasurements:
  - random1
  - random2
---
apiVersion: chantico.ci.tno.nl/v1alpha1
kind: DataCenterResource
metadata:
  name: pdu3
  namespace: default
spec:
  type: pdu
  physicalMeasurements:
  - random1
  - random2
---
apiVersion: chantico.ci.tno.nl/v1alpha1
kind: DataCenterResource
metadata:
  name: pdu2
  namespace: kube-system
spec:
  type: pdu
  physicalMeasurements:
  - random1
  - random2
---
apiVersion: chantico.ci.tno.nl/v1alpha1
kind: DataCenterResource
metadata:
  name: server1
  namespace: default
spec:
  type: server
  physicalMeasurements:
  - random1
  - random2
  parent:
  - pdu1
  - pdu2

  